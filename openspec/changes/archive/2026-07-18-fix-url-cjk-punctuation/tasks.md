# Tasks

- [x] Create vendored `crates/urlocator/` with upstream source
- [x] Add CJK/full-width punctuation to `is_illegal_at_end()`
- [x] Update root `Cargo.toml` (workspace dep → path, add workspace member)
- [x] `cargo check` passes
- [x] `cargo nextest run -p urlocator` passes
