## 任务

- [x] 根因分析：Shift+Insert 绑定被错误放在 `Integration` 频道守卫内
- [x] 移除 `init.rs:276-280` Integration 块内的 shift-insert 绑定
- [x] 在 `init.rs:330` 后（其他 `shift-*` 绑定旁）添加 shift-insert 绑定
- [x] `cargo check` 验证编译通过
